# Vapor command scripts

Files in this directory contain one Vapor CLI command per line. Blank lines and
lines beginning with `#` are ignored. Scripts stop on the first failed command
and never invoke an ambient operating-system shell.
